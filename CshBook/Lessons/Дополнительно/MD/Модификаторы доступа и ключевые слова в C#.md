# 🔐 Модификаторы доступа и ключевые слова в C#

Модификаторы помогают управлять доступом к данным и поведением объектов. Они влияют на **безопасность**, **архитектуру кода** и **неизменяемость** объектов.

---

## ✅ 1. Модификаторы доступа

| Модификатор          | Где можно использовать | Доступен…                                                    |
| -------------------- | ---------------------- | ------------------------------------------------------------ |
| `public`             | Все                    | Везде (без ограничений)                                      |
| `private`            | Все                    | Только внутри класса/структуры                               |
| `protected`          | Классы                 | Внутри класса и его наследниках                              |
| `internal`           | Все                    | В пределах текущей сборки/проекта                            |
| `protected internal` | Классы                 | В пределах сборки + в наследниках                            |
| `private protected`  | Классы                 | Только в **классе или его наследниках** внутри той же сборки |

### 📌 Пример:

```csharp
class Person
{
    public string Name { get; set; } = "Unknown";       // Доступен везде
    private int age;                                    // Только внутри Person
    protected string Status = "Active";                 // Доступен в наследниках
    internal string InternalInfo = "Internal only";     // Только в проекте
    protected internal string Hybrid = "Both";          // В проекте и наследниках
}
```

---

## 🔒 2. `const` и `readonly`

| Ключевое слово | Изменяется?             | Когда задаётся?     | Где хранится?                        |
| -------------- | ----------------------- | ------------------- | ------------------------------------ |
| `const`        | ❌ Нет                   | На этапе компиляции | В памяти программы (вшитое значение) |
| `readonly`     | ✅ Только в конструкторе | Во время выполнения | В куче/объекте                       |

### 📌 Пример:

```csharp
class Config
{
    public const double Pi = 3.1415;           // Нельзя изменить
    public readonly Guid Id;

    public Config()
    {
        Id = Guid.NewGuid();                  // Можно назначить только здесь
    }
}
```

---

## ⚙️ 3. `static`

| Что делает                           | Где применяется                |
| ------------------------------------ | ------------------------------ |
| Принадлежит **классу**, а не объекту | Классы, методы, поля, свойства |

```csharp
static class MathHelper
{
    public static int Add(int a, int b) => a + b;
}

Console.WriteLine(MathHelper.Add(2, 3)); // 5
```

*У `static class` нельзя создать объект и нельзя наследовать.*

---

## 🛠 4. `sealed`, `abstract`, `virtual`, `override`

| Ключевое слово | Назначение                                                    |
| -------------- | ------------------------------------------------------------- |
| `abstract`     | Класс/метод **без реализации**, нужно переопределить          |
| `virtual`      | Метод можно переопределить в наследнике                       |
| `override`     | Переопределение метода из базового класса                     |
| `sealed`       | Запрещает наследование класса или дальше переопределять метод |

### 📌 Пример:

```csharp
abstract class Animal
{
    public abstract void Speak();
    public virtual void Eat() => Console.WriteLine("Eating...");
}

class Dog : Animal
{
    public override void Speak() => Console.WriteLine("Woof!");

    public sealed override void Eat() => Console.WriteLine("Dog eats meat");
}

class Bulldog : Dog
{
    // Ошибка! Eat() sealed – нельзя переопределять дальше
    // public override void Eat() { }
}
```

---

## 📎 5. Полезные модификаторы

| Ключевое слово    | Для чего                           | Пример                             |
| ----------------- | ---------------------------------- | ---------------------------------- |
| `ref`             | Передача по ссылке                 | `void Do(ref int x)`               |
| `out`             | Выходной параметр                  | `bool TryParse(string s, out x)`   |
| `in`              | Передача по ссылке (только чтение) | `void Print(in int value)`         |
| `partial`         | Разделение класса/методов          | `partial class MyClass { }`        |
| `record`          | Иммутабельные объекты (C# 9)       | `public record User(string Name);` |
| `async` / `await` | Асинхронное программирование       | `async Task Run()`                 |

---

## ✅ Полный пример (всё вместе)

```csharp
public class Car
{
    public const int Wheels = 4;         // Неизменяемо
    private readonly string vin;         // Задаётся в конструкторе только 1 раз
    protected string Model { get; set; } // Видно в наследниках
    public static int Count;             // Общий счётчик для всех машин

    public Car(string model)
    {
        Model = model;
        vin = Guid.NewGuid().ToString();
        Count++;
    }

    public virtual void Drive() => Console.WriteLine("Driving...");

    public string GetInfo() => $"Model: {Model}, VIN: {vin}";
}

public sealed class Tesla : Car
{
    public Tesla() : base("Tesla") { }

    public override void Drive() => Console.WriteLine("Silent driving...");
}

// Car car = new Tesla(); // Но Tesla нельзя наследовать дальше – sealed!
```

---

## 💡 Советы

* Используй `private` по умолчанию — это принцип **инкапсуляции**.
* `const` — только для неизменяемых, универсальных значений (`Pi`, `DaysInWeek`, `MaxValue`).
* `readonly` — когда значение известно **во время выполнения**, а не заранее.
* `static` — для логики, не связанной с конкретным объектом.
* `sealed` — закрывает класс от расширения (часто в DTO, record, Entity).
* Предпочитай `is null`, `is not null` вместо `== null`.

---

## 📚 Полезные ссылки

* [Модификаторы доступа — Microsoft Docs](https://learn.microsoft.com/dotnet/csharp/programming-guide/classes-and-structs/access-modifiers)
* [readonly и const — Microsoft Docs](https://learn.microsoft.com/dotnet/csharp/language-reference/keywords/readonly)
* [static, virtual, sealed, abstract — Microsoft Docs](https://learn.microsoft.com/dotnet/csharp/language-reference)


